#!/usr/bin/env groovy

pipeline {
    agent any 
    
    stages {

        stage('build') { 
            steps {
                script{
                echo "build app"
                }
            }
        }

        stage('test') {
            
            steps {
                script{
                echo "test app"
                }
            }
        }
        stage('deploy') {
            steps {
                script {
                    def dockerComposeCmd = "docker-compose -f docker-compose.yaml up --detach"
                    sshagent(['ec2-server-key']) {
                    sh "scp -v -o StrictHostKeyChecking=no docker-compose.yaml ubuntu@54.165.143.209:/root"    
                    sh "ssh -o StrictHostKeyChecking=no ubuntu@54.165.143.209 ${dockerComposeCmd}"
}
                }
                
            }        }
}
}

