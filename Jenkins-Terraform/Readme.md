CI/CD pipline is deployed using Terraform. A docker image is build using JAR file and pushed to a new server using Terraform
Steps : 
1) Create SSH key-pair
2) Install Terraform inside Jenkins Container
3) Terraform configuration to provision server 
